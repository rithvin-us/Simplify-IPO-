# Core runtime — pure/portable, installs on Python 3.14. Pipeline runs on these.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.8
python-multipart>=0.0.9

# Optional enhancements (see requirements-optional.txt): real LLM providers and
# native document parsers/exporters. The service degrades gracefully without them
# (deterministic stub extraction/drafting, markdown/HTML export instead of .docx).
